```
usage: synflood.py [-h] -t TARGET_IP [-p TARGET_PORT] [-s SOURCE_IP] [-tf TARGET_IP_FILE] [-sf SOURCE_IP_FILE] [-c COUNT] [-i ITERATIONS]

SYN Flood Attack

options:
  -h, --help            show this help message and exit
  -t TARGET_IP, --target-ip TARGET_IP
                        Target IP address
  -p TARGET_PORT, --target-port TARGET_PORT
                        Target port number
  -s SOURCE_IP, --source-ip SOURCE_IP
                        Source IP address
  -tf TARGET_IP_FILE, --target-ip-file TARGET_IP_FILE
                        File containing target IP addresses
  -sf SOURCE_IP_FILE, --source-ip-file SOURCE_IP_FILE
                        File containing source IP addresses
  -c COUNT, --count COUNT
                        Number of packets to send
  -i ITERATIONS, --iterations ITERATIONS
                        Number of iterations
```
